---
- name: httpd_idc.serviceの設定
  systemd:
    name: httpd_idc.service
    enabled: no
    daemon_reload: no

- name: httpd_idc.serviceの開発環境自動起動設定
  systemd:
    name: httpd_idc.service
    state: started
    enabled: yes
    daemon_reload: no
  when: release_env == "dev"