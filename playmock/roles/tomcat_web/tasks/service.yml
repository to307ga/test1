---
- name: tomcat_idc.serviceの設定
  systemd:
    name: tomcat_idc.service
    enabled: no
    daemon_reload: no

- name: tomcat_idc.serviceの開発環境自動起動設定
  systemd:
    name: tomcat_idc.service
    state: started
    enabled: yes
    daemon_reload: no
  when: release_env == "dev"
