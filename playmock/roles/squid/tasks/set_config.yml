---
# squid.confの配置
- name: Copy squid.conf
  template:
    src: templates/squid.conf.j2
    dest: /etc/squid/squid.conf
    backup: yes 
    owner: root
    group: root
    mode: 0644
  notify:
    - squid_restart_msg
